<div class="memo-container py-3">
  <div class="thinking-balloon mx-auto text-center">
    <div class="mx-auto">
      <h4>選んだ理由について、なぜ選んだのか書いてみましょう</h4>
      <%= form_with model: @register_memo_form, url: memos_path, local: true, data: { turbo: false } do |f| %>
        <%= render 'shared/error_messages', object: f.object %>
        <div class="row">
          <h4 class="fw-bold">伝えたいこと</h4>
          <%= f.text_area :title, id: 'title-form', class: 'form-control w-75 mx-auto mt-3', value: @title %>
        </div>
        <div class="row mt-5">
          <h4 class="fw-bold col-6">伝えたい理由</h4>
          <h4 class="fw-bold col-6">なぜ伝えたいのか</h4>
        </div>
        <div class="row justify-content-center mt-3">
          <div class="col-5">例：人員削減になる</div>
          <div class="col-1 mt-3"><i class="bi bi-arrow-right"></i></div>
          <div class="col-5 text-start">例：AIを導入することで、社員の残業時間が1時間減ることが期待される</div>
        </div>
        <% @elements.each.with_index do |(key, value), i| %>
          <div class="row justify-content-center mx-auto mt-3">
            <div class="col-5">
              <%= f.text_area "element_#{i}", class: 'form-control mx-auto', value: value %>
            </div>
            <div class="col-1 mt-3"><i class="bi bi-arrow-right"></i></div>
            <div class="col-5">
              <%= f.text_area "basis_#{i}", class: 'form-control mx-auto' %>
            </div>
          </div>
        <% end %>
        <%= f.submit "例文を作成する", class: "example-button  text-white shadow mb-5" %>
      <% end %>  
      <div class="loading-wrap">
        <%= image_tag "loading" %>
      </div>
    </div>
  </div>
  <%= image_tag 'thinker', class: "thinker" %>
</div>