<div class="container-fluid">
  <div class="row">
    <div class="text-center col">
      <%= form_with url: memos_path, local: true do |f| %>
        <div class="my-5">
          <h3>あなたが伝えたいことはなんですか？</h3>
          <p>例：部署にAIを導入するべき</p>
          <%= f.text_field :title, class: 'form-control w-50 mx-auto' %>
        </div>
        <div class="my-5">
          <h3>伝えたい理由はなんですか？</h3>
          <p>例：人員削減になる</p>
          <div id="element-container">
            <%= f.text_field :element, id: "element-form", class: 'form-control w-50 mx-auto' %>
            </div>
            <button id="add-button" class="mt-3">入力フォームを追加</button>
        </div>
        <%= f.submit "次へ" %>
      <% end %>
    </div>
  </div>
</div>