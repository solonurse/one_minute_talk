<div class='row justify-content-around mx-auto border border-4 border-warning'>
  <div class='col-lg-12 container-fluid text-center py-3'>
    <%= form_with model: selected_memo, url: pyramid_structure_path(selected_memo), local: true, data: { turbo_method: :post } do |f| %>
      <div class="row col-10  justify-content-center ms-1">
        <div class="rounded-pill bg-danger-subtle col-2 p-1 h-25">
          <h4>主張</h4>
        </div>
      </div>
      <div class="row col-11 ps-5 mt-3">
        <%= f.text_field :title, class: "col-11 text-center", value: selected_memo.title %>
      </div>
      <div class="row">
        <div class="col-10">
          <i class="bi bi-arrow-down h2"></i>
        </div>
      </div>
      <div class="row mx-auto justify-content-around">
        <% selected_memo.explanations.each_with_index do |explanation, i| %>
          <%= f.text_area "element_#{i}", class: "col-10", value: explanation[:element] %>
        <% end %>
        <div class="rounded-pill bg-danger-subtle col-1 p-1 h-25">
          <h4>要素</h4>
        </div>
        <div class="row">
          <div class="col-10">
            <i class="bi bi-arrow-down h2"></i>
          </div>
        </div>
      </div>
      <div class="row mx-auto pb-2 justify-content-around">
        <% selected_memo.explanations.each_with_index do |explanation, i| %>
          <%= f.text_area "basis_#{i}", class: "col-10", value: explanation[:basis], rows: 3 %>
        <% end %>
        <div class="rounded-pill bg-danger-subtle col-1 p-1 h-25">
          <h4>根拠</h4>
        </div>
      </div>
      <div class="col-10 mt-2">
        <%= f.submit t('defaults.save_pyramid_structure'), class: "btn btn-primary text-center" %>
      </div>
    <% end %>
  </div>
</div>
